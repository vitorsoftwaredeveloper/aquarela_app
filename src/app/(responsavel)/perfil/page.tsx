import { PerfilScreen } from "@/features/responsavel/PerfilScreen";

/** Perfil do responsável — troca de filho, preferências, sair. */
export default function PerfilPage() {
  return <PerfilScreen />;
}
